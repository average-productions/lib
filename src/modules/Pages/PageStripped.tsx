import React, { useState, useEffect, useCallback } from "react";
import cx from "classnames";

interface Props {}

export const PageStripped: React.FunctionComponent<Props> = ({}) => {
  return <div></div>;
};
